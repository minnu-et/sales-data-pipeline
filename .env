# =============================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# =============================================================================
# This is a TEMPLATE file. Do NOT put real credentials here!
# 
# HOW TO USE:
# 1. Copy this file: cp .env.template .env
# 2. Fill in your actual values in .env
# 3. NEVER commit .env to Git (it's in .gitignore)
# 4. Share .env.template with your team (safe to commit)
# =============================================================================

# =============================================================================
# ENVIRONMENT SELECTION
# =============================================================================
# Which environment to run? (dev, prod, staging, etc.)
# Default: dev
ENV=dev

# =============================================================================
# AWS CREDENTIALS
# =============================================================================
# OPTION 1: Use AWS CLI credentials (Recommended)
# I have already run `aws configure`,so i ll leave here blank.
# The pipeline will automatically use my AWS CLI credentials
#
# OPTION 2: Provide explicit credentials (Not recommended for production)
# Only use this for local testing if AWS CLI is not configured

# AWS Access Key ID (leave blank to use AWS CLI credentials)
AWS_ACCESS_KEY_ID=

# AWS Secret Access Key (leave blank to use AWS CLI credentials)
AWS_SECRET_ACCESS_KEY=

# AWS Region (optional, defaults to config.yaml setting)
AWS_DEFAULT_REGION=ap-south-1

# =============================================================================
# S3 BUCKET CONFIGURATION
# =============================================================================
# Main S3 bucket name (overrides config.yaml if set)
# Useful if different developers use different buckets
S3_BUCKET_NAME=de-project-s3-bucket-minnu

# Backup bucket (for disaster recovery in production)
S3_BACKUP_BUCKET_NAME=de-project-s3-bucket-minnu-backup

# =============================================================================
# SPARK CONFIGURATION OVERRIDES (Optional)
# =============================================================================
# Override Spark settings from config files (useful for quick testing)

# Spark master (local, yarn, etc.)
# SPARK_MASTER=local[*]

# Driver memory
# SPARK_DRIVER_MEMORY=4g

# Executor memory
# SPARK_EXECUTOR_MEMORY=4g

# =============================================================================
# LOGGING CONFIGURATION (Optional)
# =============================================================================
# Override logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Log file path (overrides config.yaml if set)
# LOG_FILE_PATH=logs/pipeline.log

# =============================================================================
# PIPELINE BEHAVIOR (Optional)
# =============================================================================
# Force pipeline mode (overrides config.yaml)
# Options: full_refresh, incremental
# PIPELINE_MODE=full_refresh

# Enable/disable data quality checks
# ENABLE_DATA_QUALITY_CHECKS=true

# Enable/disable monitoring
# ENABLE_MONITORING=false

# =============================================================================
# DEVELOPMENT/DEBUG FLAGS
# =============================================================================
# Show DataFrames during execution (for debugging)
# Only works in dev environment
DEBUG_SHOW_DATAFRAMES=false

# Limit number of rows to process (for quick testing)
# Set to 0 for no limit
DEBUG_LIMIT_ROWS=0

# Enable Spark UI (useful for performance debugging)
# SPARK_UI_ENABLED=true

# Spark UI port
# SPARK_UI_PORT=4040

# =============================================================================
# DATA QUALITY THRESHOLDS (Optional overrides)
# =============================================================================
# Maximum allowed rejection percentage before alerting
# MAX_REJECTION_PERCENTAGE=10

# Minimum required join success percentage
# MIN_GOLD_JOIN_PERCENTAGE=70

# =============================================================================
# EXTERNAL INTEGRATIONS (Future use)
# =============================================================================
# Uncomment and fill these when you add integrations

# Slack webhook for alerts
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# Email for notifications
# ALERT_EMAIL=data-eng@company.com

# PagerDuty integration key (for critical alerts)
# PAGERDUTY_INTEGRATION_KEY=

# DataDog API key (for monitoring)
# DATADOG_API_KEY=

# =============================================================================
# DATABASE CONNECTIONS (Future use)
# =============================================================================
# If you later add a metadata database for tracking pipeline state

# PostgreSQL connection (for metadata storage)
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=pipeline_metadata
# POSTGRES_USER=admin
# POSTGRES_PASSWORD=

# Redis connection (for caching)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# =============================================================================
# SECURITY & ENCRYPTION (Production)
# =============================================================================
# KMS key for S3 encryption (production only)
# AWS_KMS_KEY_ID=

# Enable data masking for PII fields
# ENABLE_PII_MASKING=false

# =============================================================================
# NOTES FOR DEVELOPERS
# =============================================================================
# 
# IMPORTANT SECURITY RULES:
# 
# 1. ❌ NEVER commit .env to Git (it's in .gitignore)
# 2. ❌ NEVER share your .env file with anyone
# 3. ❌ NEVER hardcode credentials in code
# 4. ✅ ALWAYS use .env for secrets
# 5. ✅ ALWAYS share .env.template (it's safe - no real values)
# 6. ✅ ALWAYS rotate credentials if they're exposed
# 
# HOW TO CREATE YOUR .env FILE:
# 
# $ cp .env.template .env
# $ nano .env  # (or use any text editor)
# # Fill in your actual AWS credentials
# # Save and close
# 
# VERIFY IT'S IGNORED BY GIT:
# 
# $ git status
# # Should NOT see .env listed
# 
# IF YOU SEE .env IN GIT STATUS:
# 
# $ echo ".env" >> .gitignore
# $ git rm --cached .env  # Remove from Git if already tracked
# 
# =============================================================================
